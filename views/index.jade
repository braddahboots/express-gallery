//- recieve the mainLayout
extends ./layouts/mainLayout.jade

include ./mixins/all

block content
  .row
    .large-12.medium-12.small-12.columns
      img(src=imageGallery[0].image)
  +generateImageGallery(imageGallery)
  +generateImageGallery(imageGallery)
